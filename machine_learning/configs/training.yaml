data:
  backend: "files"
  interactions_path: "./data/delta/ecom/ml/interactions/2025-09-15/"  # adjust date
  items_path: ""  # fill later if you export items
  min_user_events: 3
  min_item_events: 5

  # optional for hybrid reads/validation
  trino:
    host: "trino"
    port: 8080
    user: "ml"
    catalog: "delta"
    schema: "silver"
    interactions_table: "orders_clean"
    items_table: ""   # fill when available

model:
  name: "mf"          # popularity | item_item | mf
  factors: 128
  iters: 20
  alpha: 40

eval:
  k: 10
  split:
    type: temporal
    val_days: 23
    test_days: 7

registry:
  mlflow_uri: "file:./mlruns"
  artifact_dir: "ml/artifacts/latest"
